const myCake = "chocolate";

// Checks whether the cake is vanilla or chocolate
if (myCake === "vanilla" || myCake === "chocolate"){
    // Checks whether the cake is not Chocolate
    if (myCake !== "chocolate"){
        console.log("This cake is vanilla");
    } else{
        console.log("This cake is chocolate");
    }
}
